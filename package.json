{
  "name": "erikg.org",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wrangler:dev": "wrangler dev --assets ./dist",
    "deploy": "npm run build && wrangler deploy",
    "deploy:preview": "npm run build && wrangler dev --assets ./dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.1",
    "vite": "^8.0.16",
    "wrangler": "^4.76.0"
  },
  "type": "module"
}
